name: 'Environment Variable Replacement'
description: 'Generate a new environment file with secrets replaced from a template file'
inputs:
  secrets:
    description: 'The json glob of all secrets to use in the replacement '
    required: true
  environment-name:
    description: 'The environment to replace variables for'
    required: true
  env-file-path:
    description: 'Convention mode: path to the final environment file to generate. It should have a sibling file with the same name but with a .<environment-name> extension. Ex. .env.staging. Optional when template-file-path is set.'
    required: false
  template-file-path:
    description: 'Optional explicit path to the template file. Overrides the <env-file-path>.<environment-name> convention. Use for files that do not follow that naming, e.g. appsettings.Production.json.'
    required: false
  delete-template:
    description: 'Whether to delete the template file after writing the output. Defaults to true so that e.g. .env.production is not left alongside .env.'
    required: false
    default: 'true'
  additional-variables:
    description: 'A json glob of additional variables to use in the replacement'
    required: false
    default: '{}'

runs:
  using: 'composite'
  steps:
    - name: Load All secrets into ENV
      shell: bash
      run: |
        EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
        to_envs() { jq -r "to_entries[] | \"\(.key)<<$EOF\n\(.value)\n$EOF\n\""; }
        echo "$SECRETS_CONTEXT" | to_envs >> $GITHUB_ENV
      env:
        SECRETS_CONTEXT: ${{ inputs.secrets }}

    - name: Load Additional Variables into ENV
      shell: bash
      run: |
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          to_envs() { jq -r "to_entries[] | \"\(.key)<<$EOF\n\(.value)\n$EOF\n\""; }
          echo "$ADDITIONAL_VARIABLES" | to_envs >> $GITHUB_ENV
      env:
          ADDITIONAL_VARIABLES: ${{ inputs.additional-variables }}

    - name: Run replacement
      shell: bash
      run: |
        ARGS=("$ENV_FILE_PATH" "$ENVIRONMENT_NAME")
        [ -n "$TEMPLATE_FILE_PATH" ] && ARGS+=("--template" "$TEMPLATE_FILE_PATH")
        [ "$DELETE_TEMPLATE" = "false" ] && ARGS+=("--no-delete-template")
        "${GITHUB_ACTION_PATH}/bin/replace" "${ARGS[@]}"
      env:
        ENV_FILE_PATH: ${{ inputs.env-file-path }}
        ENVIRONMENT_NAME: ${{ inputs.environment-name }}
        TEMPLATE_FILE_PATH: ${{ inputs.template-file-path }}
        DELETE_TEMPLATE: ${{ inputs.delete-template }}
